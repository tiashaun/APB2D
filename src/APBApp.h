#ifndef _APBAPP_H_#define _APBAPP_H_#include <SDL.h>#include "APBSurface.h"#include "APBEvent.h"#include "APBAnimation.h"#include "APBSprite.h"#include "APBSpriteManager.h"class APBApp : public APBEvent{private:    // States    bool running;    bool paused;    // Display settings    int scr_w;    int scr_h;    int scr_bpp;    SDL_Surface* surfDisplay;    SDL_Surface* surfTest;    APBAnimation NumberTestSprite;public:    APBApp();    int execute();public:    bool init();    int setDisplayProperties(int w, int h, int bpp);    void event(SDL_Event* Event);    void loop();    void render();    void cleanup();    // Actionspublic:    void togglePause();    // Eventspublic:    void onExit();    void onKeyDown(SDLKey sym, SDLMod mod, Uint16 unicode);};#endif