#ifndef _ABPSURFACE_H_#define _ABPSURFACE_H_#include <SDL.h>#include <SDL_image.h>class APBSurface {public:    APBSurface();public:    static SDL_Surface* load( char* File );    static bool draw( SDL_Surface* dst, SDL_Surface* src, int x, int y );    static bool draw( SDL_Surface* dst, SDL_Surface* src, int x1, int y1, int x2, int y2, int w, int h );    static bool setTransparentColor( SDL_Surface* dst, int r, int g, int b );};#endif