#ifndef APBSPRITEMANAGER_H_#define APBSPRITEMANAGER_H_#include <SDL.h>class APBSpriteManager {public:    APBSpriteManager();    ~APBSpriteManager();    int cleanup();    int pushSprite();    int popSprite();    void animate();    void update();    //std::vector getDirtyRectangles();private:    //std::vector<SDL_Rect> dirtyRectangles;};#endif // APBSPRITEMANAGER_H_